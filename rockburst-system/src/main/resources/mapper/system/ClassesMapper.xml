<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.ClassesMapper">

    <select id="selectClassesList" resultType="com.ruoyi.system.domain.vo.ClassesVO">
        select
        c.classes_id as classesId,
        c.classes_name as classesName,
        c.create_time as createTime,
        c.update_time as updateTime,
        c.del_flag as delFlag
        from classes as c
        <where>
            <if test="classesSelectDTO.classesName != null and classesSelectDTO.classesName != ''">
                and c.classes_name like concat('%',#{classesSelectDTO.classesName},'%')
            </if>
            <if test="classesSelectDTO.startTime != null">
                and c.create_time &gt;= #{classesSelectDTO.startTime}
            </if>
            <if test="classesSelectDTO.endTime != null">
                and c.create_time &lt;= #{classesSelectDTO.endTime}
            </if>
            and c.del_flag = '0' and c.mine_id = #{mineId}
        </where>
        order by c.create_time desc
    </select>
</mapper>